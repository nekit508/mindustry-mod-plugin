nmp.tools(project, "tools", nmp.project.core)

tools.configure {
    genericInit()

    configureTasks {
        nmptRunTools {
            mainClass.set "tools.Tools"
        }
    }
}

dependencies {
    implementation(nmp.arcDependency "v146", "arc-core")
}